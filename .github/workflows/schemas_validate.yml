name: schemas-validate
on: [push, pull_request]
jobs:
  jsonschema:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: pip install jsonschema
      - name: Validate examples
        run: |
          python - <<'PY'
          import json
          from jsonschema import validate
          tap = json.load(open('examples/measurement/tap_peaks.json')); validate(tap, json.load(open('contracts/schemas/tap_peaks.schema.json')))
          moe = json.load(open('examples/measurement/moe_result.json')); validate(moe, json.load(open('contracts/schemas/moe_result.schema.json')))
          man = json.load(open('examples/measurement/manifest.json')); validate(man, json.load(open('contracts/schemas/manifest.schema.json')))
          print("All measurement examples validate.")
          PY
