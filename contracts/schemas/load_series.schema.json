{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://tap-tone-pi.local/contracts/schemas/load_series.schema.json",
  "title": "Load Time Series",
  "description": "Load cell force readings over time.",
  "type": "object",
  "additionalProperties": false,

  "required": [
    "schema_id",
    "schema_version",
    "artifact_type",
    "created_utc",
    "unit",
    "sample_rate_hz",
    "data"
  ],

  "properties": {
    "schema_id": { "const": "load_series" },
    "schema_version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+$",
      "description": "Schema version (e.g. '1.0')"
    },

    "artifact_type": { "const": "load_series" },
    "created_utc": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when series was captured"
    },

    "unit": {
      "type": "string",
      "enum": ["N", "lbf", "kgf"],
      "description": "Force unit"
    },
    "sample_rate_hz": {
      "type": "number",
      "exclusiveMinimum": 0,
      "description": "Sampling rate in Hz (samples per second)"
    },

    "sensor": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional load cell metadata",
      "properties": {
        "type": { "type": "string", "enum": ["strain_gauge", "piezo", "hydraulic", "other"] },
        "capacity_N": { "type": "number", "exclusiveMinimum": 0 },
        "resolution_N": { "type": "number", "exclusiveMinimum": 0 },
        "serial_number": { "type": "string" },
        "calibration_date": { "type": "string", "format": "date" }
      }
    },

    "data": {
      "type": "array",
      "description": "Array of [time_s, force] pairs",
      "items": {
        "type": "array",
        "prefixItems": [
          { "type": "number", "description": "Time in seconds from start" },
          { "type": "number", "description": "Force in specified unit" }
        ],
        "minItems": 2,
        "maxItems": 2
      },
      "minItems": 1
    },

    "statistics": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional computed statistics",
      "properties": {
        "min": { "type": "number" },
        "max": { "type": "number" },
        "mean": { "type": "number" },
        "std": { "type": "number", "minimum": 0 },
        "n_samples": { "type": "integer", "minimum": 1 }
      }
    },

    "sha256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "SHA256 hash of raw data file (if applicable)"
    }
  }
}
