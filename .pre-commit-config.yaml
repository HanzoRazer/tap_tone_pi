# Pre-commit hooks for tap_tone_pi
# Install: pip install pre-commit && pre-commit install
# Run manually: pre-commit run --all-files

repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.14.10
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
      - id: ruff-format

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      # Archived source material is recorded by SHA-256 and must survive
      # byte-for-byte. A formatter that strips trailing whitespace from an
      # archived patch silently invalidates the digest that identifies it - and
      # in a diff, trailing whitespace on a context line is significant.
      - id: trailing-whitespace
        exclude: ^docs/reference/acquisition/
      - id: end-of-file-fixer
        exclude: ^docs/reference/acquisition/
      - id: check-yaml
        exclude: ^\.github/workflows/schema-registry-guard\.yml$
      - id: check-json
      - id: check-added-large-files
        args: [--maxkb=1000]

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.10.0
    hooks:
      - id: mypy
        additional_dependencies:
          - "numpy>=1.24,<2.0"
          - types-setuptools
        args: [--config-file=mypy.ini, --follow-imports=silent]
        files: ^tap_tone_pi/
